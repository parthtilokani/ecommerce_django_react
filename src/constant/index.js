import {COLORS, FONTSIZE, SHADOWS} from './theme';
import {Dimensions} from 'react-native';
import icons from './icons';
const {width, height} = Dimensions.get('window');

export {icons, COLORS, FONTSIZE, SHADOWS, width, height};
